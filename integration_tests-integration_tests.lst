       |﻿@trusted:
       |
       |import std.getopt;
       |
       |import dpq2;
       |import conn = dpq2.connection: _integration_test;
       |import query = dpq2.query: _integration_test;
       |import result = dpq2.result: _integration_test;
       |import native = dpq2.conv.native_tests: _integration_test;
       |import bson = dpq2.conv.to_bson: _integration_test;
       |
       |int main(string[] args)
       |{
      1|    string conninfo;
      1|    getopt( args, "conninfo", &conninfo );
       |
      1|    conn._integration_test( conninfo );
      1|    query._integration_test( conninfo );
      1|    result._integration_test( conninfo );
      1|    native._integration_test( conninfo );
      1|    bson._integration_test( conninfo );
       |
      1|    return 0;
       |}
integration_tests/integration_tests.d is 100% covered
