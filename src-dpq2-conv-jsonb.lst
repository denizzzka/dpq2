       |///
       |module dpq2.conv.jsonb;
       |
       |@safe:
       |
       |import vibe.data.json;
       |import dpq2.value;
       |import dpq2.oids: OidType;
       |
       |package:
       |
       |import std.string;
       |import std.conv: to;
       |
       |///
       |Json jsonbValueToJson(in Value v)
       |{
0000000|    assert(v.oidType == OidType.Jsonb);
       |
0000000|    if(v.data[0] != 1)
0000000|        throw new ValueConvException(
       |            ConvExceptionType.CORRUPTED_JSONB,
       |            "Unknown jsonb format byte: "~v._data[0].to!string,
       |            __FILE__, __LINE__
       |            );
       |
0000000|    string s = (cast(const(char[])) v._data[1 .. $]).to!string;
       |
0000000|    return parseJsonString(s);
       |}
src/dpq2/conv/jsonb.d is 0% covered
